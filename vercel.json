{
  "buildCommand": "NODE_OPTIONS=--openssl-legacy-provider npm install && NODE_OPTIONS=--openssl-legacy-provider npm run build",
  "outputDirectory": "build",
  "env": {
    "NODE_OPTIONS": "--openssl-legacy-provider",
    "GENERATE_SOURCEMAP": "false",
    "SKIP_PREFLIGHT_CHECK": "true"
  }
}
